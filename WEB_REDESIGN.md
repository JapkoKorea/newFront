# 웹 재설계 기획 + 와이어프레임 (Commerce-first)

비에이 택시투어 **웹(Next.js `src/`)** 페이지 점검 결과와 개편 방향.
방향성은 `UX_REDESIGN.md`(모바일 기준, 2026-06-19 확정)의 commerce-first 하이브리드를
**웹에 맞게 구체화**한 것이다. 시각 디자인(색/타이포)은 제외, 구조/플로우/와이어프레임까지만.

와이어프레임 표기 규칙(wireframe-spec): 회색 박스 = 콘텐츠 블록, `[ X ]` = 이미지,
`~~~` = 텍스트 블록, `(n)` = 콘텐츠 우선순위(로드/노출 순서), `->` = 인터랙션/이동.
별점은 `[별점]`, 찜(하트 버튼)은 `[찜]`으로 표기(프로젝트 이모지 금지 규칙).

---

## 1. 현재 웹 상태 진단 (코드 근거)

완성도: **기능 65~70%** — 개별 기능은 거의 동작, **구조 정리 + 커머스 개편**이 핵심 과제.

### 1.1 라우트별 상태

| 라우트 | 상태 | 비고 |
|--------|------|------|
| `/` 홈 | 동작 | 풀페이지 스크롤 애니메이션. **커머스 요소(상품카드/가격/후기) 없음** |
| `/tours`, `/tours/[id]` | 동작 | 9개 코스 SSG + 경로 에디터. **가격/후기/찜 없는 텍스트 리스트** |
| `/products` | **고아** | 커머스형 목록 구현됨, **네비게이션 링크 없음 -> 도달 불가** |
| `/products/[slug]` | **고아** | 9개 섹션 커머스 상세(잘 만들어짐). 도달 불가 |
| `/booking` | 동작 | `TaxiBooking.jsx` 4단계 모달. **1059줄 거대 컴포넌트** |
| `/transfer-booking` | 동작 | 송영 예약 (별도 플로우) |
| `/pricing` `/guide` `/reservations` `/login` `/payments/*` | 동작 | 정상 |

### 1.2 핵심 이슈 (우선순위순)

1. **[높음] `/products` 카탈로그가 고아.** `Navigation.jsx`에 링크가 없어 접근 불가.
   -> 잘 만든 커머스 상세페이지가 사장돼 있음. **네비 연결 + 홈 노출이 최우선.**
2. **[높음] tours vs products 이중화.** 두 개의 코스 시스템이 공존.
   - `tourCourses`(9개, 텍스트 리스트 + 지도 빌더) vs `products`(2개, 커머스 상세 + 가격표)
   - 관계 정의 필요(3장 권고안 참조).
3. **[높음] 홈이 커머스가 아님.** 풀페이지 스크롤 랜딩. 상품 피드/가격/후기/검색 없음.
   `UX_REDESIGN.md` 2장 원칙과 어긋남.
4. **[중간] Navigation 모바일 메뉴 누락.** 데스크탑엔 있는 항목 일부만 반영, 정렬 불일치.
5. **[중간] `TaxiBooking.jsx` 1059줄.** step별 서브컴포넌트 분해 필요(유지보수).
6. **[낮음] `/profile` 없음.** 모바일엔 프로필 있음. 웹은 마이페이지 부재.
7. **[낮음] 홈 휠 스크롤 하이재킹**(`src/app/page.jsx:37`) — 접근성/성능 리스크.

> 정정: 초기 조사에서 "상품 이미지 경로 오류"로 지목됐으나 **오탐**.
> `heroImage: '/assets/*.jpg'` -> `public/assets/*.jpg` 정상 매핑, 파일 존재 확인.

### 1.3 무관/오탐 항목

- 이미지 경로: 정상.
- stash 4개(`feat/view`/`feat/reserve`/`main` WIP): 현재 작업과 무관.

---

## 2. tours vs products 정리 — 권고안

두 시스템의 역할을 **명확히 분리**한다. 대체가 아니라 **책임 분담**.

| 시스템 | 역할(개편 후) | 데이터 소스 |
|--------|--------------|------------|
| `products/` | **커머스 카탈로그** — 목록/상세(마케팅·가격·후기·SEO). 홈 피드 소스. | `src/products/` -> 추후 DB `courses` |
| `tours/[id]` | **지도 빌더** — 경로 커스터마이징(출발/도착/관광지 reorder). | 기존 Google Maps 빌더 재사용 |

흐름: **홈(상품피드) -> `/products/[slug]`(커머스 상세) -> "이 코스로 예약"(빌더 or 바로 일정) -> `/booking`(일정/연락처/동의) -> 결제.**

- `products` 상품 데이터에 `cta.courseId`가 이미 있음 -> 상세에서 지도 빌더(`/tours/[id]`)로 프리필 진입 가능.
- `/tours` 목록은 **`/products`로 리디렉션 또는 통합**(중복 목록 제거). 9개 코스는 순차적으로 `products/items/`로 이관.
- 마이그레이션 중에는 products(2개) + tours 폴백 병행. `registry.js` 조회만 API로 교체하면 렌더 재사용(README의 DB 항목 참조).

---

## 3. 개편 후 정보구조 (웹 IA)

```
글로벌 네비 (상단 고정)
  로고 | 투어 상품 | 관광 가이드 | 요금 안내 | 예약 확인 | [카카오 로그인/마이]
  로그인    "투어 상품" = /products (기존 "투어 코스" 링크 대체)

홈 /
  = 커머스 랜딩: 히어로 + 검색("어디로 가세요?") + 시즌/카테고리 필터
    + 추천 상품 카드 피드(사진/가격/후기/찜/badge) + "직접 코스 만들기" CTA
    + 송영(공항) 배너 + 가이드/신뢰 섹션 + 푸터

/products            상품 목록(커머스 그리드 + 필터/검색)
/products/[slug]     상품 상세(현행 유지 + 후기/찜/가격 강화)
/tours/[id]          지도 빌더(커스터마이징/직접 만들기) — 상세에서 진입
/booking             일정·인원 -> 예약자정보 -> 요금·동의 (지도 제거)
/transfer-booking    송영 견적 플로우 (별도)
/reservations /pricing /guide /login /payments/*   유지
```

인증 게이트: 진입이 아니라 **예약자 정보 -> 요금·동의 전환 지점**에서만 카카오 로그인.

---

## 4. 와이어프레임 (웹, 데스크탑 + 반응형)

### 4.1 홈 `/` — 커머스 랜딩 (데스크탑 1024px 이상)

```
+----------------------------------------------------------------+
| 로고 잽코    투어상품  관광가이드  요금안내  예약확인  [로그인]   | (1) 상단 고정 네비
+----------------------------------------------------------------+
|  [        히어로 이미지 / 비에이 풍경 (16:9)        ]           | (2) 히어로
|   비에이를 택시로, 원하는 대로.                                 |     H1 + 서브카피
|   +--------------------------------------------------+         |
|   | (돋보기) 어디로 가세요?  [ 날짜 ] [ 인원 ] [검색] |         | (3) 검색바
|   +--------------------------------------------------+         |     -> /products?q=
|                                                                |
|  [전체] [겨울] [여름] [사계절] [인기]                           | (4) 필터칩 -> 피드 필터
|                                                                |
|  추천 상품                                        [전체보기 >] | (5) 섹션 헤더 -> /products
|  +----------------+  +----------------+  +----------------+    |
|  | [ 사진4:3 ][찜] |  | [ 사진 ][특가] |  | [ 사진 ][찜]  |    | (6) 상품 카드 그리드(3열)
|  | 겨울 설경 드라이브|  | 여름 라벤더투어|  | 청의호수 힐링  |    |     카드 = Link -> /products/[slug]
|  | 비에이역·약6시간  |  | 후라노·3~6시간 |  | ...            |    |     사진+제목+요약
|  | [별점]4.9 (128)  |  | [별점]4.8 (86)|  | [별점]—       |    |     별점/후기수(placeholder 허용)
|  | 예약금 15,000원  |  | 6,500엔/시간~ |  | ...            |    |     가격 노출
|  +----------------+  +----------------+  +----------------+    |
|                                                                |
|  +--------------------------------------------------+         |
|  |  + 원하는 코스가 없나요? 직접 코스 만들기          |         | (7) 빈 빌더 진입 -> /tours/new
|  +--------------------------------------------------+         |
|                                                                |
|  +--------------------------------------------------+         |
|  | 공항 송영이 필요하세요?  신치토세<->호텔  [예약 >] |         | (8) 송영 배너 -> /transfer-booking
|  +--------------------------------------------------+         |
|                                                                |
|  왜 잽코인가 | 한국어 상담 · 현지결제 · 유연한 코스변경        | (9) 신뢰 섹션(3열 아이콘)
|  관광 가이드 미리보기  [ 가이드 카드 x3 ]  [더보기 >]          | (10) 가이드 -> /guide
+----------------------------------------------------------------+
|  푸터: 사업자정보 · 정책 · 카카오채널 · FAQ                     | (11)
+----------------------------------------------------------------+
| (플로팅) [상담] [FAQ]  우하단 고정                              |
+----------------------------------------------------------------+
```

반응형:
- **태블릿 768~1023px**: 상품 카드 2열, 검색바 세로 확장.
- **모바일 768px 미만**: 카드 1열, 네비 -> 햄버거. 하단 고정 CTA("예약하기") 노출 고려.
  모바일 레이아웃은 `UX_REDESIGN.md` 6.1과 동일 원칙.

상태:
- **로딩**: 카드 자리에 skeleton(`ui/skeleton.jsx`) 3~6개.
- **빈 상태**(필터 결과 0): "해당 조건의 상품이 없어요 + 직접 만들기" CTA.
- **후기 데이터 없음**: 별점 영역 "준비중" 또는 숨김(placeholder 허용).

콘텐츠 소스: (6) 상품 카드 = `products/registry.js` `listProducts()`(현재 정적, 추후 API).

---

### 4.2 상품 목록 `/products` — 커머스 그리드

```
+----------------------------------------------------------------+
| ...네비...                                                     |
+----------------------------------------------------------------+
|  투어 상품                                                     | (1) H1
|  +--------------------------------------------------+         |
|  | (돋보기) 코스/지역 검색           [겨울][여름][전체]|         | (2) 검색 + 필터
|  +--------------------------------------------------+         |
|  정렬: [추천순 v]                              12개 상품       | (3) 정렬 + 카운트
|                                                                |
|  +--------+ +--------+ +--------+ +--------+                   | (4) 카드 그리드
|  |[사진][찜]| |[사진]  | |[사진][찜]| |[사진]  |    (4열/데스크탑)|     홈 카드와 동일 컴포넌트
|  |제목     | |제목    | |제목     | |제목    |                   |     ProductCard 재사용
|  |[별점]6h | |특가·3h | |...      | |...     |                   |
|  |15,000원 | |6,500엔~| |...      | |...     |                   |
|  +--------+ +--------+ +--------+ +--------+                   |
|  ... (grid 반복)                                               |
+----------------------------------------------------------------+
```

반응형: 데스크탑 4열 / 태블릿 2~3열 / 모바일 1~2열.
현행 `src/app/products/page.jsx`가 이미 grid + `<img>` 렌더 -> **카드에 가격/별점/찜 추가**만 하면 됨.

---

### 4.3 상품 상세 `/products/[slug]` — 현행 강화

현행 `ProductDetailClient.jsx`(9섹션: 히어로/하이라이트/코스/요금/예약절차/규칙/정책/FAQ/리뷰이벤트)는
**구조 양호**. 커머스 요소만 보강.

```
+----------------------------------------------------------------+
| ...네비...                                                     |
| 홈 > 상품 > 여름 라벤더 택시투어                        [공유]  | (1) 브레드크럼 + 공유(신규)
| [                히어로 이미지 (16:9)              ]  [ 찜 ]   | (2) 히어로 + 찜(신규)
| 여름 라벤더 택시투어                          [여름 한정]       | (3) 제목 + badge
| [별점] 4.8 (86) · 3~6시간 · 비에이/후라노                       | (4) 별점(신규)+메타
| --------------------------------------------------------------- |
| 하이라이트 ~~~   | 코스 스팟 · 요금표 · 예약절차 · 정책 · FAQ  | (5~) 기존 섹션 유지
| --------------------------------------------------------------- |
| 후기 (신규 섹션)  [별점]5 홍길동  "친절하고..."   [더보기]      | (N) 후기 리스트(신규)
+----------------------------------------------------------------+
| [ 하단 고정 바 ] 예약금 15,000원~  [ 커스터마이징 ] [ 예약하기 ]| (마지막) sticky CTA
+----------------------------------------------------------------+
     커스터마이징 -> /tours/[cta.courseId] (지도 빌더)
     예약하기     -> saveTourBookingPrefill + /booking
```

보강 항목(신규): 공유 버튼(카카오 공유), 찜(하트, 로컬->서버), 별점/후기 섹션,
하단 sticky CTA 바(모바일 필수). 나머지는 현행 유지.

---

### 4.4 지도 빌더 `/tours/[id]` (+ `/tours/new` 빈 상태) — 2-pane

`UX_REDESIGN.md` 6.6 확정 레이아웃. 기존 Google Maps 빌더 재사용, booking에서 지도 제거.

```
+----------------------------------------------------------------+
| ...네비...                                                     |
+---------------------------------+------------------------------+
|                                 | 경로 만들기                  | (1) 우측 패널
|                                 | O 출발 [ 비에이역        v ] |     Places 자동완성
|          [   지 도   ]          | O 도착 [ 아사히카와공항   v ] |
|        (경로선 + 마커)           | --------------------------- |
|                                 | 추천 관광지 [검색.........]  | (2) 관광지 추가
|         (2) 마커 클릭 ->         | 1. 청의 호수           [x]  |     reorder/삭제
|             관광지 추가/제거      | 2. 흰수염폭포          [x]  |
|                                 | 3. 사계채언덕          [x]  |
|                                 | --------------------------- |
|                                 | 예상 6시간 · 관광지 3곳      | (3) 요약
|                                 | [    다음: 일정·인원    ]   | (4) -> /booking
+---------------------------------+------------------------------+
```

- `/tours/new` = 빈 상태(출발/도착 미지정) 진입 -> "직접 코스 만들기".
- `/tours/[id]` = 추천 코스 프리필 진입("커스터마이징").
- 모바일: 지도 전체화면 + 드래그 바텀시트(6.3).

상태: 지도 로드 실패 -> 안내 + 텍스트 폼 폴백. Places 무응답 -> 재시도.

---

### 4.5 예약 `/booking` — 일정/연락처/동의 (지도 제거)

`TaxiBooking` 4단계에서 **1단계 코스/경로 제거**(빌더가 담당) -> 3단계로 축소.

```
+----------------------------------------------------------------+
| 예약하기                                            1 / 3      | (1) 스텝 인디케이터
| 경로 요약: 비에이역 -> 아사히카와공항 · 관광지 3곳 · 6시간     | (2) 빌더에서 넘어온 요약(읽기전용)
| --------------------------------------------------------------- |
| [1] 일정·인원   날짜 [ ] 시간 [ ] 인원 [-2+] 이용시간 [-6+]     | (3) step1
| [2] 예약자 정보  이름(영문) [ ] 연락처 [ ]  (미로그인->카카오)   | (4) step2 = 인증 게이트
| [3] 요금·약관    예상요금/예약금 표 + 동의 체크                 | (5) step3 -> 결제
| [                    다음                    ]                 |
+----------------------------------------------------------------+
```

구현: `TaxiBooking.jsx`(1059줄)를 `steps/ScheduleStep`/`ContactStep`/`AgreementStep`로 분해.
지도/코스 선택 코드 제거 -> 빌더로 이관.

---

### 4.6 Navigation 개편

```
데스크탑:  로고 | 투어 상품 | 관광 가이드 | 요금 안내 | 예약 확인 | [로그인/마이]
           투어상품 href = /products (기존 "투어 코스" -> /tours 대체)
모바일(햄버거):  홈 · 투어 상품 · 관광 가이드 · 요금 안내 · 예약 확인 · 예약하기 · FAQ · 상담 · [로그인]
           데스크탑과 항목 동일하게 정렬(현재 누락분 보강)
```

---

## 5. 구현 우선순위 (제안)

| 순위 | 작업 | 범위 | 근거 |
|------|------|------|------|
| P0 | `/products` 네비 연결 + "투어 상품" 링크 | `Navigation.jsx` | 고아 페이지 즉시 해소, 저비용 고효과 |
| P0 | 홈 -> 커머스 카드 피드(추천 상품) 섹션 추가 | `app/page.jsx` + `ProductCard` | 커머스-first 핵심 |
| P1 | `ProductCard` 공통 컴포넌트(사진/가격/별점/찜) | 신규 `components/ProductCard.jsx` | 홈·목록 재사용 |
| P1 | 상품 상세 sticky CTA + 공유 + 찜(로컬) | `ProductDetailClient.jsx` | 전환율 |
| P1 | tours 목록 -> products 통합/리디렉션 결정 | 라우팅 | 이중화 해소 |
| P2 | `/tours/[id]` 빌더화 + `/tours/new` 빈 상태 | 빌더 재정비 | 지도 단일화 |
| P2 | `/booking` 3단계 축소 + 컴포넌트 분해 | `TaxiBooking.jsx` | 유지보수 |
| P2 | 후기·별점 백엔드(`DB_DESIGN.md` 6장) | BE + FE | 커머스 신뢰요소 |
| P3 | 9개 tourCourses -> products/items 이관 | 데이터 | 카탈로그 단일화 |
| P3 | `/profile` 마이페이지, 휠스크롤 개선 | 신규/개선 | 완성도 |

---

## 6. 열린 결정 (사용자 확인 필요)

1. **tours 목록 처리**: `/tours` 목록을 `/products`로 완전 대체(리디렉션)할지, 당분간 병행할지.
   (권고: 대체 + 9코스 순차 이관)
2. **후기 데이터**: 실제 후기 시스템 착수 시점(지금 placeholder vs BE 먼저).
3. **찜 범위**: 1차 로컬스토리지만 vs 서버 동기화까지.

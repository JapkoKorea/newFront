# 상담 채팅 구조 기획

비에이 택시투어 상담을 "채팅"으로 진행하기 위한 아키텍처 검토. 웹/모바일 모두,
앱 설치 없이, 가장 가볍고 유저가 편한 방식을 찾는다.

---

## 1. 현재 상태

- **`src/components/ChatSupport.jsx`**: 하드코딩 봇 응답만 있는 **목업**. 백엔드 없음, 실시간 아님.
- **이 저장소 백엔드**: 메시징/상담 API 없음 (payments / reservations / auth 뿐).
- **이미 있는 자산**:
  - 카카오 OAuth 로그인 (웹 + 모바일, JWT) — 유저 식별 가능.
  - `pf.kakao.com/_japko` 카카오 채널.
  - **japko_local DB를 별도 LINE/카카오 챗 백엔드(japko/Backend)와 공유** — 이게 핵심 지렛대.

---

## 2. 핵심 질문 정리

| 질문 | 답 |
|------|-----|
| 앱 없이 로그인만으로 메시지 주고받기 가능? | **가능.** 그게 웹 인앱챗(로그인 기반). 모바일 브라우저 그대로 동작. |
| 앱처럼 쓰게 하려면? | **PWA**("홈 화면에 추가")로 설치감 + 웹 푸시. 앱스토어 불필요. |
| 가장 큰 난관은? | **유저가 채팅창을 떠난 뒤 답장 알림** (실시간 전송보다 이 알림이 UX를 가른다). |

---

## 3. 옵션 비교

### A. 웹 인앱챗 (자체 구축, 로그인 기반)
로그인한 유저가 사이트 내 채팅 위젯으로 대화 → 메시지 DB 저장 → 관리자가 대시보드에서 응답.

- 장점: **완전 커스텀**(예약 맥락 연동 가능), 앱 설치 불필요, 모든 브라우저, 이미 있는 auth+DB 재사용.
- 단점: 관리자 응답 UI를 만들어야 함. 유저가 떠나면 답장을 모름 → **알림 채널 필요**.

### B. 카카오톡 채널 (비즈니스)
`pf.kakao.com/_japko`로 연결.

- 장점: 한국 유저 100% 보유, 설치 0, **네이티브 푸시 알림**, 익숙함.
- 단점: **UI 커스텀 거의 불가**, 카카오 생태계 종속, 자동화하려면 상담톡/챗봇 API + 비즈 인증 필요. (당신이 번거롭다고 한 부분)

### C. PWA + 웹 푸시
A(웹 인앱챗)에 PWA 설치 + 웹 푸시(Service Worker) 결합.

- 장점: 앱 없이 앱 경험 + 떠나 있어도 답장 푸시. 커스텀 유지.
- 단점: iOS 웹 푸시는 "홈 화면 추가" 후에만 동작(iOS 16.4+). 커버리지 제약.

### D. 하이브리드 (권고) — 웹 인앱챗 + 카카오 알림
평상시 대화는 웹 인앱챗(커스텀), **"답장 도착" 알림만 카카오 알림톡/웹푸시**로.

- 장점: 커스텀 UI(A) + 확실한 재유입 알림(B의 강점). 두 마리 토끼.
- 단점: 알림톡은 템플릿 승인 + 발송 비용. 초기엔 웹푸시/이메일로 시작 가능.

---

## 4. 추천 아키텍처

**하이브리드(D)**: 웹 인앱챗을 메인 상담 창구로, 알림은 카카오/웹푸시로 보강.
그리고 **메시지 저장/중계는 이미 있는 shared DB(japko_local) + LINE/카카오 챗 백엔드를 재사용**한다.

```
[유저: 웹/모바일 브라우저]
  로그인(카카오, 이미 있음)
        |
        v
  웹 인앱챗 위젯 (ChatSupport 교체)
        |  REST + (폴링/SSE)
        v
  [백엔드 메시지 API]  ── 저장 ──> [japko_local DB (공유)]
        ^                              |
        |                              | 같은 DB를 보는
   관리자 응답                          v
   (1) 웹 관리자 대시보드   또는   (2) 기존 LINE/카카오 챗 백엔드
                                    = 관리자는 익숙한 카카오/LINE에서 답장,
                                      유저는 웹 인앱챗에서 확인 (양방향 동기화)
        |
        v
  [답장 도착 알림]  웹푸시(우선) / 카카오 알림톡(후속)
```

**왜 이 구조인가**
- 유저: 앱 설치 0, 카카오 로그인 한 번, 브라우저에서 바로 상담. 가장 가벼움.
- 당신: 웹 UI를 완전 커스텀(A의 장점). 동시에 **관리자는 기존 카카오/LINE 툴에서 답장**할 수 있어(공유 DB 덕분) 별도 대시보드 없이도 운영 가능 — 당신이 편함.
- 카카오 채널 커스텀의 번거로움(B 단점)을 피하면서, 카카오는 **알림 용도로만** 얇게 사용.

---

## 5. 기술 선택 — 실시간 전송 방식

| 방식 | 실시간성 | 구현 난이도 | 권고 |
|------|---------|------------|------|
| **폴링(polling)** | 3~5초 지연 | 매우 쉬움 | **1단계 채택** — REST `GET /messages?after=` 주기 호출 |
| SSE (Server-Sent Events) | 즉시(서버→클라 단방향) | 쉬움 | 2단계 업그레이드 |
| WebSocket | 즉시 양방향 | 중간 | 트래픽 커지면 |

상담은 콜센터가 아니라 **비동기 문의**에 가까우므로(응답이 몇 분 걸릴 수 있음) **폴링으로 시작**해도 충분. 알림이 재유입을 책임지므로 실시간성 요구가 낮다.

---

## 6. 데이터 모델 (초안)

`DB_DESIGN.md`와 정렬. shared DB에 대화/메시지 테이블(기존 LINE/카카오 챗 백엔드와 스키마 호환 확인 필요).

```
conversations
  id, user_id(카카오/서비스 유저), channel('web'|'kakao'|'line'),
  reservation_id(nullable, 예약 맥락 연동), status('open'|'closed'), created_at, updated_at

messages
  id, conversation_id, sender('user'|'admin'|'bot'|'system'),
  body, attachments(nullable), read_at(nullable), created_at
```

- `reservation_id`로 "어떤 예약 문의인지" 맥락 연결 → 상세/예약내역에서 "문의하기" 진입.
- `channel`로 웹/카카오/LINE 통합 인박스 구현 가능.

---

## 7. 구현 로드맵 (제안)

1. **DB/스키마 정렬**: 기존 japko/Backend 챗 스키마 확인 → conversations/messages 재사용 or 신설.
2. **메시지 API** (이 저장소 backend): `POST /api/chat/messages`, `GET /api/chat/messages?after=`, `POST /api/chat/conversations`. JWT 인증.
3. **웹 인앱챗 교체**: `ChatSupport.jsx`의 목업을 실 API + 폴링으로 교체. 비로그인 시 "카카오 로그인 후 상담" 유도(결제 전 로그인 정책과 동일 톤).
4. **관리자 응답 경로**: (a) 최소 관리자 페이지 or (b) 기존 LINE/카카오 챗 백엔드에서 답장 → 공유 DB 통해 웹 반영.
5. **알림**: 1차 웹푸시(Service Worker) or 이메일 → 2차 카카오 알림톡(템플릿 승인 후).
6. **PWA**: manifest + Service Worker로 "홈 화면 추가" + 푸시. (선택)
7. **예약 맥락 연동**: 상품 상세/예약내역에 "이 예약 문의하기" → 해당 reservation_id로 대화 생성.

---

## 8. 확정 사항 (2026-07-03 결정)

- **관리자 응답: 기존 카카오/LINE 챗 백엔드 재사용.** 공유 DB(japko_local)를 통해 관리자는 익숙한 카카오/LINE에서 답장 → 웹 인앱챗에 반영. 별도 웹 관리자 대시보드는 만들지 않음(운영 부담 최소화).
- **상담 시작: 로그인 후 상담.** 카카오 로그인해야 메시지 가능. 유저 식별·이력·알림이 확실해지고, 결제 전 로그인 정책과 톤 일치. 비로그인 시 "카카오 로그인 후 상담" 유도.
- **알림 1차 수단: 웹푸시(Service Worker).** 무료·즉시. iOS는 "홈 화면 추가"(PWA) 후 동작(16.4+)하므로 PWA와 함께 진행. 후속으로 카카오 알림톡 검토.

### 남은 세부 결정
- **봇 자동응답 유지 여부**: 현재 목업 FAQ 자동응답을 1차 봇으로 남길지(대기시간 완충) vs 전부 사람 상담. (구현 중 결정 가능)

---

## 9. 확정 아키텍처 요약

```
[유저 웹/모바일] --카카오 로그인--> [웹 인앱챗 위젯]
   REST + 폴링(3~5초)
        v
[이 저장소 backend: /api/chat/*] --저장--> [japko_local 공유 DB]
                                              ^
                                              | 같은 DB
[관리자] 기존 카카오/LINE 챗 백엔드에서 답장 ----+
        v
[웹푸시] 유저 재유입 (+ PWA 홈화면 추가)
```

첫 구현 단계(로드맵 2~3): shared DB 챗 스키마 확인 → `/api/chat` 메시지 API →
`ChatSupport.jsx` 목업을 실 API + 폴링으로 교체(로그인 게이트 포함).

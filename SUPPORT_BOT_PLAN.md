# 상담 봇 기획

웹 인앱챗의 1차 응대와 운영자 전달 흐름을 정의한다.

---

## 1. 왜 필요한가

웹 채팅은 메시지를 저장하는 데까지는 동작했지만 **읽는 쪽이 없었다.**
CHAT_PLAN 에서는 관리자가 카카오·LINE 백엔드에서 답장하기로 했으나,
japko/Backend 는 `conversations`·`messages` 테이블을 쓰지 않는다(LINE API 로 직접 응답).
문의를 남겨도 아무도 보지 못하는 상태였다.

그래서 두 가지를 함께 만들었다.

1. **봇** — 자주 묻는 것에 즉시 답한다
2. **운영자 인박스** — 봇이 답하지 못한 문의를 확인하고 답변한다

---

## 2. 봇의 역할

**LLM 을 쓰지 않는다.** 요금·기한처럼 지어내면 안 되는 사실을 다루므로,
정책 상수에서 문장을 만든다. 답할 수 없는 것은 답하지 않고 운영자에게 넘긴다.

### 답하는 주제

| 주제 | 근거 |
|---|---|
| 시간당 요금 | `HOURLY_RATE_JPY` (src/lib/pricing.js 와 같은 값) |
| 예약금 | `BASE_DEPOSIT_KRW` / `LAST_MINUTE_DEPOSIT_KRW` 환경변수 |
| 취소·환불 | `refund_service.FULL_REFUND_DAYS` |
| 변경 기한 | `change_request_service` 의 날짜/시간 기준일 |
| 차량·인원 | 5인 이상 점보, 캐리어 규칙 |
| 겨울 사륜구동 | 적설기 배차 정책 |
| 결제 방법 | 예약금은 온라인, 택시 요금은 현지 현금 |
| 소요 시간 | 최소 2시간, 1시간 단위 추가 |
| 코스 | 시즌별 상품 안내 |

규칙은 **좁은 주제를 먼저** 검사한다. "겨울에 눈길 괜찮나요"가 '요금'보다
'겨울'에 먼저 잡혀야 하기 때문이다.

숫자 인원 표현(`6명인데 어떤 차가`)은 정규식으로 잡되, 가까이에 차량을 가리키는
말이 있을 때만 차량 주제로 본다. `2명인데 얼마인가요`는 요금 질문이다.

### 답하지 못하면

"문의 내용을 담당자에게 전달했습니다"라고 알리고 운영자 대기열로 넘긴다.
고객이 답을 기다려도 되는지 알 수 있어야 한다.

---

## 3. 운영자 인박스

### 미답변 판정

별도 상태 컬럼을 두지 않는다. **마지막 고객 메시지 이후 운영자 답변이 없으면
미답변**이다. 봇 응답은 답변으로 치지 않는다.

상태 컬럼을 따로 두면 메시지와 어긋날 수 있어, 메시지에서 직접 계산한다.

### API

```
GET  /api/admin/conversations?unanswered=true&limit=100
  200 → { conversations: [...] }
       각 항목에 예약자 이름, 마지막 메시지, 발신자, 메시지 수, 미답변 여부

GET  /api/admin/conversations/{id}/messages?after=0
  200 → { messages: [...] }

POST /api/admin/conversations/{id}/messages
  body: { body }
  200 → { message }   sender='admin' 으로 저장, 고객 화면에 폴링으로 반영
  404 → 없는 대화
```

권한은 `users.role = 'admin'`이며 매 요청 DB 에서 읽는다.

---

## 4. 화면

### 고객

- 상담 위젯에서 발신자를 구분해 표시한다. 봇은 **자동 안내**, 사람은 **상담원**.
- 봇 응답은 메시지 전송 응답에 함께 실려 와서 폴링을 기다리지 않고 바로 보인다.
- 예약 확인 화면의 **문의하기**가 예약 맥락(`reservation_number`)을 붙여 상담을 연다.
  어느 예약에 대한 문의인지 대화에 기록된다.

### 운영자

화면은 아직 없다. API 만 있어 직접 호출해 처리한다.

---

## 5. 카카오 채널과의 병행

`conversations.channel` 컬럼이 이미 있어 통합 인박스로 갈 준비가 돼 있다.

| 채널 | 맡을 일 | 이유 |
|---|---|---|
| 웹 인앱챗 | 로그인 고객의 예약 맥락 문의, 당일 변경 요청 | reservation_number 를 붙일 수 있는 유일한 채널 |
| 카카오 채널 | 비로그인 사전 문의, 예약 확정·변경 알림톡 | 로그인이 카카오라 도달률이 가장 높음 |

지금은 웹만 이 인박스를 쓴다. 카카오·LINE 을 같은 인박스에 넣으려면
japko/Backend 에 어댑터가 필요하다(다음 단계).

---

## 6. 다음 단계

1. **운영자 화면** — 인박스 목록과 대화창. 지금은 API 만 있다.
2. **예약 정보 추출** — japko/Backend 의 `reservation_kakaobiz` 에이전트를 재사용해
   대화에서 날짜·인원·코스를 뽑아 예약 초안을 만든다. LLM 키와 비용이 발생한다.
3. **카카오 채널 어댑터** — 통합 인박스
4. **알림** — 운영자 답변 시 고객에게 알림톡. 템플릿 승인 리드타임이 있다.

---

## 7. 알아둘 것

**요금표가 두 곳에 있다.** `src/lib/pricing.js`(정본)와
`backend/services/support_bot_service.py`의 `HOURLY_RATE_JPY`가 같은 값을
들고 있다. 프론트와 백엔드가 나뉜 구조에서 불가피하지만, 요금을 바꿀 때
두 곳을 함께 고쳐야 한다. 장기적으로는 DB 나 API 로 한 곳에서 내려주는 것이 낫다.

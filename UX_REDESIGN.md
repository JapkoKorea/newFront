# UX 재설계 — Commerce-first 하이브리드 (모바일 기준)

비에이 택시투어 예약 서비스의 첫 화면/진입 플로우 재설계.
이 제품은 "호출형(ride-hailing)"이 아니라 **여행 액티비티 상품 예약**(선결제 예약금, 4~6시간)이다.
따라서 한국 여행 커머스 앱(마이리얼트립/클룩/트리플/야놀자) 트렌드에 맞춰
**홈은 상품(추천 코스) 우선, 지도 인터페이스는 필요할 때(직접 만들기/커스터마이징) 등장**시킨다.
지도 위 위치 선택 UI(카카오 T 스타일)는 버리지 않고 빌더 단계로 위치만 옮긴다.
본 문서는 정보구조/플로우/와이어프레임까지만. 시각 디자인(색/타이포/간격)은 추후.

> 설계 히스토리: 1차안은 "map-first(지도를 홈으로)"였으나, 한국에서 map-first는
> 카카오T·쏘카 같은 호출형 컨벤션이고 여행 상품 카테고리와 어긋난다고 판단해
> commerce-first 하이브리드로 전환(2026-06-19 결정).

---

## 1. 현재 상태 진단 (코드 기준)

### 모바일 (Flutter, `mobile/lib/`)
- 라우팅(`app/app_router.dart`): `initialLocation` = 비로그인 시 `/login`, 로그인 시 `/tours`.
  - 라우트: `/login`, `/auth/callback`, `/tours`(홈), `/tours/:id`(상세), `/booking`(3단계), `/reservations`, `/payment/*`.
  - 하단 탭 네비게이션이 없음. 화면 이동이 AppBar 아이콘에 의존.
- 홈 = `ToursPage`(`/tours`): 시즌 코스 카드 리스트인데 **사진·가격·후기·찜이 없는 맨 텍스트 리스트**. 안내문도 "시즌별 코스를 먼저 선택하고..."로 코스 선택을 강제. 필터칩 라벨에 눈/꽃 이모지.
- `/tours/:id` = `TourDetailPage`: **카카오T 스타일의 좋은 지도 빌더.** 전체화면 지도 + 상단 경로 선택 바(출발 -> 도착, Places 자동완성) + 드래그 바텀시트(코스 검색, 관광지 reorder/삭제, "예약 입력 페이지로 이동").
- `/booking` = `BookingShellPage`: 3단계 폼.
  - 1단계 `routeAndSchedule`: **또 다른(더 조악한) 지도** `MapPickerCard`(모드칩 + 탭-확정 + 하드코딩 관광지) + 날짜/시간/인원/시간.
  - 2단계 `contact`: 이름/연락처. 3단계 `pricingAgreement`: 예상요금/예약금/규칙/동의 -> `/payment/review`.

### 웹 (Next.js, `src/`)
- `TaxiBooking.jsx` 4단계 모달: **코스 선택 -> 일정 -> 경로 -> 연락처.** 코스 선택이 1단계.
- 송영(transfer)은 별도 `/transfer-booking`(`TransferBooking.jsx`).

### 핵심 문제
1. **홈이 빈약하다.** 코스를 먼저 보여주는 것 자체는 이 카테고리의 정석이지만, 사진·가격·후기·찜·특가 같은 한국 커머스 필수 요소가 없는 맨 리스트라 "구려" 보인다. 문제는 "코스가 먼저"가 아니라 "홈이 상품으로 안 꾸며짐".
2. **지도 UI 이중화.** `TourDetailPage`(좋은 빌더)와 `BookingShellPage`의 `MapPickerCard`(조악)가 공존 → 경로를 두 번 만지게 됨. 중복 + 일관성 붕괴.
3. **하단 네비 부재.** 홈/예약내역/마이 상시 이동 경로 없음.
4. **인증 강제.** 탐색 전에 `/login` 강제. 한국 커머스는 비로그인 탐색 후 결제 직전 간편로그인이 표준.
5. 이모지 사용(브랜드 톤 규칙 위반).

---

## 2. 설계 원칙

- **Commerce-first 홈.** 첫 화면 = 추천 코스 상품 피드(사진/가격/후기/찜/시즌 badge) + 상단 검색("어디로 가세요?"). 마이리얼트립/클룩 톤.
- **Map-on-demand.** 지도(카카오T 스타일 빌더)는 홈이 아니라 "직접 코스 만들기" 또는 코스 상세의 "커스터마이징"에서 등장.
- **코스는 출발 템플릿, 게이트가 아니다.** 추천 코스를 고르면 경로가 프리필된 빌더로 진입. 안 고르고 "직접 만들기"로 빈 빌더 진입도 가능.
- **지도 UI 단일화.** `TourDetailPage` 패턴이 유일한 지도 빌더. `MapPickerCard`(booking 1단계) 폐기.
- **인증은 결제 직전.** 예약자 정보 -> 요금·동의 전환에서만 카카오 로그인.
- **지도가 받는 건 booking에서 다시 묻지 않는다.** booking = 일정/인원/연락처/동의만.
- **한국 커머스 요소 탑재.** 후기·별점, 찜(하트), 가격 노출, 특가/시즌 badge, 카카오톡 공유.

---

## 3. 새 정보구조 (IA)

하단 탭 셸:

```
앱
├── [탭1] 홈      (기본) = 추천 코스 커머스 피드 + 검색
├── [탭2] 예약내역 = reservations
└── [탭3] 마이     = 로그인/로그아웃, 찜 목록, 정책, 고객지원
```

화면 목록(모바일):
- 홈: 검색바("어디로 가세요?") + 시즌/카테고리 필터 + 추천 코스 카드 피드(사진/가격/후기/찜/badge) + "직접 코스 만들기" 진입
- 코스 상세: 사진/일정/포함내역/후기/가격 + "이 코스로 예약"(빌더로) / "커스터마이징"
- 지도 빌더(직접 만들기 / 커스터마이징): 지도 + 상단 경로 바 + Places 검색 + 관광지 reorder (= 기존 TourDetailPage 패턴)
- 일정·인원 / 예약자 정보(카카오 프리필) / 요금·약관 동의 -> 결제(review/success/fail)
- 예약내역 / 마이(찜 목록 포함)

코스 데이터: 현 `tourCourses`를 홈 피드 + 추천 소스로 사용. 공유 딥링크 `/tours/:id` -> 코스 상세.

---

## 4. 플로우

```
[앱 실행]  (비로그인 허용)
   v
[홈: 추천 코스 피드 + 검색]
   |                         \
   | (코스 카드 탭)            (직접 코스 만들기 / 검색)
   v                           v
[코스 상세] --(이 코스로)--> [지도 빌더] <-- 빈 상태에서 직접 경로 생성
   |  사진/후기/가격          |  출발/도착/관광지 확정 (Places 검색 or 지도)
   |  (커스터마이징)----------/
   v
[일정·인원]   날짜/시간/인원/이용시간
   v
[예약자 정보] --(미로그인)--> [카카오 로그인] --> 복귀
   v
[요금·약관 동의]
   v
[예약 접수 -> 결제(review)]
   v
[결제 성공] --> [예약내역]
```

분기:
- 추천 코스 경로: 홈 -> 코스 상세 -> (그대로 예약 or 커스터마이징) -> 일정·인원 ...
- 직접 만들기 경로: 홈 -> 빌더(빈 상태) -> 경로 생성 -> 일정·인원 ...
- 송영(transfer): **별도 진입**(홈 배너/서비스 메뉴). 코스/관광지 없이 공항<->호텔 + 일정 -> 기사 견적 -> 수락 -> 결제. 상세는 9장.

---

## 5. 액세스 패턴

| 진입점 | 도착 화면 | 인증 요구 |
| --- | --- | --- |
| 콜드 스타트 | 홈(코스 피드) | 불필요(탐색) |
| 코스 카드 탭 | 코스 상세 | 불필요 |
| 직접 코스 만들기 / 검색 | 지도 빌더 | 불필요 |
| 코스 상세/빌더 -> 다음 | 일정·인원 | 불필요 |
| 예약자 정보 "다음" | 요금·동의 | **여기서 카카오 로그인 강제** |
| 요금·동의 -> 결제 | 결제 review | 필요(이미 로그인) |
| 하단 탭 예약내역 | 예약내역 | 필요(미로그인 시 로그인 유도) |
| 찜(하트) | 마이>찜 목록 | 로컬 허용, 서버 동기화는 로그인 |
| 딥링크 `/tours/:id` | 코스 상세 | 불필요 |

핵심 변경: 인증 게이트가 진입점(`/login`)에서 **예약자 정보 -> 요금·동의 전환 지점**으로 이동.

---

## 6. 와이어프레임 (구조만, 시각 디자인 제외)

### 6.1 모바일 홈 (commerce-first)
```
+-----------------------------+
| 비에이 택시투어              |
| +-------------------------+ |
| | (돋보기) 어디로 가세요?   | |   검색 -> 빌더(직접 만들기)
| +-------------------------+ |
| [전체] [겨울] [여름] [인기]  |   시즌/카테고리 필터(텍스트)
|                             |
| 추천 코스                    |
| +-------------------------+ |
| | [      사 진      ] [찜] | |   코스 카드: 사진 + 찜(하트 버튼)
| | 겨울 설경 드라이브        | |
| | 비에이역 출발 · 약 6시간   | |
| | 별점 4.9 (128)            | |   후기/별점
| | 예약금 15,000원           | |   가격 노출
| +-------------------------+ |
| +-------------------------+ |
| | [      사 진      ] [특가]| |   badge
| | 청의 호수 힐링 코스       | |
| | ...                       | |
| +-------------------------+ |
| +-------------------------+ |
| |   + 직접 코스 만들기      | |   빈 빌더 진입
| +-------------------------+ |
| [홈]    [예약내역]    [마이] |   하단 탭
+-----------------------------+
```

### 6.2 모바일 코스 상세
```
+-----------------------------+
| [<]                   [공유] |
| [        사 진/지도        ] |
| 겨울 설경 드라이브           |
| 별점 4.9 (128) · 약 6시간    |
| 예약금 15,000원              |
| --------------------------- |
| 코스 경로                    |
|  비에이역 -> 1.청의호수 ...   |   (미니 지도 + 순서)
| 포함/불포함 · 후기           |
| --------------------------- |
| [ 커스터마이징 ] [ 이 코스로 ]|   좌:빌더 편집 / 우:바로 일정으로
+-----------------------------+
```

### 6.3 모바일 지도 빌더 (직접 만들기 / 커스터마이징) = 기존 TourDetailPage 패턴
```
+-----------------------------+
| [<]  O 출발 [비에이역      ] |   상단 경로 바 (탭 -> Places 검색)
|      O 도착 [아사히카와공항 ] |
|        [ 지도 + 경로선 ]     |
| +-------------------------+ |
| |          --             | |   바텀시트
| | 관광지 추가 [검색]        | |
| | 1. 청의 호수         [x] | |   reorder / 삭제
| | 2. 흰수염폭포        [x] | |
| | [   다음: 일정·인원   ]  | |
| +-------------------------+ |
+-----------------------------+
```

### 6.4 모바일 일정·인원
```
+-----------------------------+
| [<]  일정 및 인원      1 / 3 |
| 경로 요약                    |
|  비에이역 -> 아사히카와공항   |
|  관광지 2곳 · 예상 6시간      |
| --------------------------- |
| 날짜     [ 2026-07-12    v] |
| 시간     [ 09:00         v] |
| 인원     [ - ]  2  [ + ]    |
| 이용시간 [ - ]  6  [ + ]    |
|          [   다음   ]       |
+-----------------------------+
```
이후 2/3 예약자 정보(카카오 프리필), 3/3 요금·약관 동의 -> 결제.

### 6.5 하단 탭 셸
```
+-----------------------------+
|        (현재 탭 내용)        |
+-----------------------------+
| [홈]     [예약내역]    [마이] |
+-----------------------------+
```

### 6.6 웹 — 2-pane (코스 상세/빌더에서)
```
+----------------------------------------------+
| 로고    투어  가이드  요금    [예약내역][로그인]|
+--------------------------+-------------------+
|                          | 경로 만들기        |
|         지 도            | O 출발 [비에이역  ]|
|      (경로 + 마커)        | O 도착 [공항      ]|
|                          | 추천코스 [겨울][여름]|
|                          | 관광지 1.청의호수[x]|
|                          |        2.폭포   [x]|
|                          | 일정 [날짜][시간]  |
|                          | 인원 [2] 시간 [6]  |
|                          | [ 다음: 연락처·결제]|
+--------------------------+-------------------+
```
웹 홈은 SEO용 랜딩/코스 목록(SSG) 유지하되 커머스 카드(사진/가격/후기)로 정렬. 예약은 위 전용 2-pane 페이지.

---

## 7. 마이그레이션 단계 (구현 순서 제안)

1. 하단 탭 셸(홈/예약내역/마이) 도입 + 진입을 홈으로(로그인 게이트 제거).
2. 홈(`ToursPage`)을 커머스형 피드로 개편: 상단 "어디로 가세요?" 검색 + 시즌/카테고리 필터 + 코스 카드(사진/가격/후기/찜/badge) + "직접 코스 만들기" CTA. 이모지 제거.
3. 지도 빌더 역할 정리: `TourDetailPage` 패턴을 "직접 만들기/커스터마이징" 화면으로(빈 상태 진입 지원). `/booking` 1단계 `MapPickerCard` 폐기.
4. `/booking` = 일정/인원/연락처/동의만. `applyTourPrefill` 확장(출발/도착/관광지/좌표).
5. 인증 게이트를 결제 직전으로 이동(`skipAuth`/redirect 정리).
6. 커머스 요소: 찜(우선 로컬+서버 동기화), 후기·별점·가격 노출(데이터 소스 필요 — 후기는 백엔드 준비 전 placeholder 가능).
7. 웹: 전용 2-pane 예약 페이지 + 홈 커머스 카드 정렬.
8. (Phase 2) 송영 서비스 토글 + 관리자 견적 연동.

---

## 8. 결정 사항 / 남은 질문

확정 (2026-06-19):
- **홈 방향: commerce-first 하이브리드.** 추천 코스 피드 + 검색 홈, 지도는 직접 만들기/커스터마이징에서.
- **인증: 비로그인 탐색 허용, 결제 직전에서만 카카오 로그인.**
- **웹 예약: 전용 2-pane 페이지로**(기존 `TaxiBooking` 모달 대체).
- **후기·별점: 실제 시스템 구축**(로컬 MySQL). 스키마는 `DB_DESIGN.md` 6장.
- **송영(transfer): 별도 진입**(홈 토글 아님). 기사 견적 확답 필요 → 견적 플로우. 아래 9장.
- **직접 코스 만들기(빈 빌더): 1차 포함.** 추천 코스 없이도 본인 경로 즉시 생성 가능.

남은 질문(세부): `DB_DESIGN.md` 10장 참고(코스 시드 출처, 후기 사진 저장소, 기사 계정 운영 방식 등).

---

## 9. 송영(transfer) 별도 플로우

송영은 투어와 성격이 다르다: **공항<->호텔 점대점 이동**이라 관광지/코스/루트 빌더가 불필요하고,
요금이 거리/차량/시간에 따라 달라 **기사 확답(견적)** 후에야 확정된다. 그래서 홈 토글이 아니라
별도 진입(홈 배너 또는 마이/서비스 메뉴의 "공항 송영")으로 둔다.

홋카이도 실제 패턴(신치토세 공항 CTS <-> 호텔)을 기준으로 입력을 단순화:
- **방향 토글**: 공항 -> 호텔 / 호텔 -> 공항 (한 번에 결정, 두 화면 안 만듦)
- **공항**: 기본 신치토세(CTS), 선택지로 아사히카와(AKJ) 등
- **호텔**: Places 검색(자유 입력)
- **항공편명**(선택): 픽업 타이밍 산정용
- **인원 + 수하물 수**: 차량(밴) 크기 산정에 중요 → 견적 정확도

### 9.1 송영 플로우
```
[송영 예약 진입]
   방향 토글 + 공항(기본 CTS) + 호텔(Places) + 날짜/시간 + 항공편(선택) + 인원/수하물 + 요청사항
   v
[견적 요청 제출]   status: requested   (미로그인이면 여기서 카카오 로그인)
   v
[기사 견적 대기]  --알림-->
   v
[견적 확인]  이용요금(JPY) / 예약금(KRW) / 기사 메모 / 유효기간
   |                         \
   | 수락                      거절
   v                           v
[예약금 결제] -> confirmed   status: declined
```
상태: `requested -> quoted -> accepted -> paid -> confirmed`(거절 declined / 만료 expired / 기사거절 rejected).
데이터: `transfer_details` + `reservation_quotes`(견적 이력) — `DB_DESIGN.md` 5장. 관리자 견적 입력은
`TODO_ADMIN_QUOTE.md`와 동일 시스템.

### 9.2 와이어프레임 — 송영 입력
```
+-----------------------------+
| [<]  공항 송영 예약          |
| [ 공항->호텔 | 호텔->공항 ]   |   방향 토글
| 공항   [ 신치토세(CTS)    v] |
| 호텔   [ 호텔명 검색       ] |   Places
| 날짜   [ 2026-07-12      v] |
| 시간   [ 14:30           v] |   (도착편 기준 안내문)
| 항공편 [ JL3047 (선택)     ] |
| 인원 [-] 3 [+]  수하물 [-]2[+]|
| 요청사항 [                 ] |
| [      견적 요청하기      ]  |
+-----------------------------+
```

### 9.3 와이어프레임 — 견적 확인(예약 상세)
```
+-----------------------------+
| 송영 #1234        상태: 견적도착|
| 신치토세공항 -> OO호텔        |
| 7/12 14:30 · 3명 · 수하물 2  |
| --------------------------- |
| 기사 견적                    |
|  이용요금  18,000엔          |
|  예약금    15,000원          |
|  메모: 대형 밴 배차          |
|  유효기간 ~ 7/8 23:59        |
| [   거절   ] [ 수락하고 결제 ]|
+-----------------------------+
```
